import 'package:flutter/material.dart';

class CarTrips extends StatelessWidget {
  const CarTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Car trips'),
    );
  }
}
