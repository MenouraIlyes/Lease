import 'package:flutter/material.dart';

class LikesCars extends StatelessWidget {
  const LikesCars({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Liked Cars'),
    );
  }
}
